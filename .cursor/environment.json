{
  "name": "Carbonio Bayes Trainer",
  "user": "ubuntu",
  "install": "sudo apt-get update && sudo apt-get install -y python3-venv && python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -e '.[dev]'"
}
